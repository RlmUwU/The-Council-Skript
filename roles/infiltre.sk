on load:
    set {_role.name} to "infiltre"
    set {_role.displayname} to "infiltré"
    add {_role.name} to {roles::*}
    set {%{_role.name}%.camp} to "infiltre"
    add {_role.name} to {infiltrate.roles::*}
    set {%{_role.name}%.order} to 0 